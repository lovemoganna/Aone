# Agent Studio 流程编排可视化板块能力补全与升级方案

基于对当前 `aone-toolkit` 中 `Visual Flow Editor` 的代码分析（`FlowEditor.svelte`, `FlowNode.svelte`, `FlowEdge.svelte` 等），结合 MECE 原则，制定以下 21 项能力补全与升级计划。

当前状态概要：
- **已实现**：基础拖拽、节点连接、撤销重做（HistoryStore）、基础调试工具栏（Mock）、框选、复制粘贴。
- **缺失/薄弱**：辅助对齐、复杂逻辑可视化（循环/异常）、生产级调试数据、变量引用体验、工程化复用。

本方案旨在从**体验、逻辑、调试、工程**四个维度进行系统性升级。

---

## 一、 交互体验与操作效率 (UX & Efficiency)

### 1. 智能吸附与辅助对齐 (Smart Alignment Guidelines)
*   **问题**：当前节点拖拽仅支持 20px 网格吸附，难以实现精细的视觉对齐，导致排版杂乱。
*   **实现**：
    *   拖拽时自动检测相邻节点的 X/Y 轴与中心点。
    *   显示动态辅助线（X/Y 轴延伸线）与距离提示。
    *   吸附阈值（如 5px）内自动吸附。
*   **收益**：排版效率提升 50%，显著提升图表的整洁度与专业感。

### 2. 画布智能小地图 (Interactive Minimap Navigation)
*   **问题**：现有 `FlowMinimap` 仅作展示，不支持点击跳转或拖拽视口。
*   **实现**：
    *   支持在小地图上点击任意位置，主画布视口立即平滑滚动至该点。
    *   支持在小地图上拖拽视口框（Viewport Frame）进行快速漫游。
*   **收益**：大图（>20节点）浏览效率提升，解决“迷路”痛点。

### 3. 连接线智能路由 (Smart Orthogonal Routing)
*   **问题**：当前使用简单贝塞尔曲线（Simple Bezier），节点交错时线条会穿过节点实体，视觉干扰严重。
*   **实现**：
    *   引入路径规划算法（如 A* 或简单的 Manhattan Routing）。
    *   实现“正交路由（Orthogonal Routing）”模式，线条自动避开节点 Bounding Box。
    *   支持“智能避让”开关。
*   **收益**：解决复杂流程图的“面条代码（Spaghetti Code）”视觉问题，提升逻辑可读性。

### 4. 快捷键可视化面板 (Keyboard Shortcuts Overlay)
*   **问题**：快捷键（Ctrl+C/V/Z）隐性存在，用户学习成本高，易遗忘。
*   **实现**：
    *   新增 `?` 或键盘图标按钮，唤起快捷键浮层。
    *   可视化展示当前支持的所有操作（含 Shift+Click, Space+Drag 等）。
    *   上下文敏感：仅显示当前模式下可用的快捷键。
*   **收益**：降低新手学习门槛，加速专家用户操作，明确显性化隐藏能力。

### 5. 画布自动布局 (Auto Layout)
*   **问题**：导入或粘贴大量节点后，需要手动长时间整理布局。
*   **实现**：
    *   集成 `dagre` 或 `elkjs` 布局算法库。
    *   提供“一键整理”按钮，支持“水平流”、“垂直流”两种模式。
    *   保留动画过渡，让用户理解布局由于变化。
*   **收益**：瞬间将混乱流程结构化，工程与演示场景必备功能。

---

## 二、 逻辑编排与复杂度管理 (Logic & Orchestration)

### 6. 可折叠子流程 (Collapsible Sub-flows / Groups)
*   **问题**：随着业务复杂度增加，单画布节点数激增（>50），心智负担过重，无法聚焦。
*   **实现**：
    *   新增 `GroupNode` 类型，支持将选中的多个节点“打包”收起。
    *   支持双击进入子画布（钻取模式）或原地展开/折叠。
    *   子流程对外暴露合并后的 Input/Output 端口。
*   **收益**：支持超大规模流程编排，符合“黑盒”抽象思维，降低视觉噪声。

### 7. 逻辑节点可视化构建器 (No-code Condition Builder)
*   **问题**：`Condition` 节点当前仅依赖 JSON/String 表达式，非技术人员难以编写正确逻辑。
*   **实现**：
    *   属性面板增加“条件构建器” UI。
    *   下拉选择：`Variable` -> `Operator` (==, >, contains) -> `Value`。
    *   自动生成底层 JS 表达式代码。
*   **收益**：降低非技术人员由于语法错误导致的流程阻塞，实现真正的 No-Code 体验。

### 8. 可视化循环容器 (Visual Loop Container)
*   **问题**：当前缺乏明确的循环（For Each/While）语义，依赖回连线条，逻辑不直观且易死循环。
*   **实现**：
    *   新增 `LoopScope` 容器节点，包裹需要重复执行的节点组。
    *   视觉上区分“循环体”区域。
    *   内置 `index`, `item` 等标准循环变量供内部节点引用。
*   **收益**：将隐式逻辑显性化，大幅降低循环逻辑的错误率。

### 9. 异常处理边界 (Error Boundary / Try-Catch Block)
*   **问题**：节点出错通常导致流程中断，缺乏统一的兜底机制，需每个节点单独处理。
*   **实现**：
    *   支持为节点或节点组配置 `On Error` 端口或边界框。
    *   当区域内发生 Error，自动路由至该端口连接的“异常处理流程”。
*   **收益**：提升生产环境流程的稳定性与健壮性，简化容错逻辑编排。

### 10. 变量引用自动补全 (Visual Variable Picker)
*   **问题**：节点输入需要手动输入 `{{step1.output}}`，极易输错且需记忆上游节点 ID。
*   **实现**：
    *   在输入框输入 `{{` 或 `{` 时触发浮层。
    *   解析上游链路，列出所有可用的节点及其 Output Schema。
    *   点击即插入正确引用路径。
*   **收益**：解决“数据流断裂”的核心痛点，将变量引用错误率降至接近 0。

### 11. 动态端口管理 (Dynamic Node Ports)
*   **问题**：当前节点端口（Input/Output）可以固定，不支持根据配置动态增减（如 Switch Case 多分支）。
*   **实现**：
    *   允许特定节点（如 Router, Parallel）在 UI 上点击 `+` 添加输出端口。
    *   右键端口可删除或重命名。
*   **收益**：支持 Switch-Case 等多分支逻辑的灵活编排。

---

## 三、 调试与可观测性 (Debugging & Observability)

### 12. 实时数据流可视化 (Real-time Data Flow Overlay)
*   **问题**：调试时不知道节点间传递了什么数据，完全“盲跑”。
*   **实现**：
    *   Debug 模式下，执行过的 Edge 线条变色。
    *   悬停 Edge 显示具体传输的数据包（Payload）快照。
    *   线条粗细或动画速率反映数据量大小或处理耗时（可选）。
*   **收益**：调试透明度显著提升，从“猜数据”变为“看数据”。

### 13. 执行时光机 (Execution Time-Travel / Replay)
*   **问题**：流程跑完后，无法回溯查看中间某一瞬间的状态。
*   **实现**：
    *   Debug 工具栏增加“时间轴滑块”。
    *   拖动滑块，画布节点状态（Running/Completed/Output）同步回退或快进。
    *   基于 `Event Sourcing` 或快照日志实现。
*   **收益**：复杂逻辑排错神器，通过慢动作回放几分钟前的状态来定位偶发 Bug。

### 14. 节点状态透视镜 (Node State Inspector)
*   **问题**：节点执行后的 Input/Output/Logs 散落在日志面板，难以与图形对应。
*   **实现**：
    *   选中节点时，侧边栏或悬浮窗展示该节点“最近一次截图”的完整 Context。
    *   包含：Input Variables, Output Result, Execution Time, Error Stack。
*   **收益**：所见即所得的调试体验，减少视线在“画布”与“控制台”间跳跃。

### 15. 性能热力图 (Performance Profiler Overlay)
*   **问题**：无法直观感知哪个步骤是性能瓶颈。
*   **实现**：
    *   执行结束后，根据耗时渲染节点颜色深浅（Heatmap）。
    *   标注每个节点的具体耗时（ms/s）。
*   **收益**：快速定位 LLM 响应过慢或 I/O 阻塞的环节，辅助性能优化。

### 16. 断点条件配置 (Conditional Breakpoints)
*   **问题**：当前断点是无条件的，循环 100 次时每次都断，调试效率低。
*   **实现**：
    *   右键断点，配置 `Condition: index == 50`。
    *   仅当满足条件时暂停流程。
*   **收益**：专业级调试能力，解决长流程、循环流程的调试阻塞。

---

## 四、 工程化与生态集成 (Engineering & Ecosystem)

### 17. 流程片段库 (Snippet / Template Library)
*   **问题**：常用的“鉴权逻辑”、“重试逻辑”无法跨流程复用，需重复搭建。
*   **实现**：
    *   选中一组节点 -> 右键 "Save as Snippet"（存为片段）。
    *   左侧组件栏增加 "My Snippets" 面板，拖拽即可复用整块逻辑。
*   **收益**：实现逻辑复用，沉淀团队的最佳实践资产。

### 18. 可视化版本比对 (Visual Diff)
*   **问题**：多人协作或版本回滚时，无法直观看出 V1 和 V2 改了哪根线、哪个参数。
*   **实现**：
    *   利用 `Graph Diff` 算法。
    *   以高亮（红删绿增黄改）形式叠加显示在画布上。
    *   侧栏列出变更清单（Change List）。
*   **收益**：解决“不知道谁改了什么”的协作黑洞，提升变更评审效率。

### 19. 智能补全建议 (AI Visual Autocomplete)
*   **问题**：用户拖出一个 "HTTP Request" 节点后，往往不知道下一步该接什么。
*   **实现**：
    *   从端口通过拖拽出线条松开到空白处时，弹出“推荐节点”菜单。
    *   基于上下文（例如上游是 URL String，推荐 Http Client；上游是 List，推荐 Loop）。
*   **收益**：降低新手迷茫感，提升编排流畅度 (Flow State)。

### 20. 标注与便利贴 (Sticky Notes)
*   **问题**：流程图缺乏文档解释能力，逻辑意图全靠猜。
*   **实现**：
    *   提供独立于执行逻辑的“注释节点”或“背景区域”。
    *   支持 Markdown 文本，用于解释复杂的业务规则。
*   **收益**：代码即文档（Diagram as Documentation），提升可维护性。

### 21. 流程导出/截图 (Export to Image/SVG)
*   **问题**：无法方便地分享流程设计到文档或汇报 PPT 中。
*   **实现**：
    *   利用 `html-to-image` 或 Canvas API。
    *   支持“导出选中区域”或“导出全图”为 PNG/SVG。
    *   自动添加水印或项目信息。
*   **收益**：提升汇报与文档编写效率，增强系统的传播价值。

---

## 自检总结 (Self-Check)

1.  **数量检查**：共提出 21 项增强点，满足 >20 的要求。
2.  **MECE 检查**：覆盖了交互（1-5）、核心逻辑（6-11）、调试（12-16）、工程（17-21）四个互斥且完整的维度。
3.  **重复性检查**：“智能补全”与“动态端口”无重叠；“子流程”与“循环容器”职能分离。
4.  **落地性检查**：每项均对应成熟的前端技术方案（如 ReactFlow/SvelteFlow 生态插件、Dagre 布局、Monaco Editor 集成等）。
5.  **空泛性检查**：所有点均包含具体的 UI/Interaction 描述，拒绝了“提升智能性”等模糊描述。

此方案可直接作为 Next Phase 的 Feature Backlog 进行排期开发。
